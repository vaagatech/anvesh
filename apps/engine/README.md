# Anvesh Engine

Search API package (`@vaagatech/anvesh-engine`) — BM25, semantic, hybrid, and geo search.

```bash
# from monorepo root
npm run dev:engine
```

Defaults: `ANVESH_STORAGE=filesystem`, data in `.anvesh/data/`.

By [VaagaTech](https://www.vaagatech.com).
